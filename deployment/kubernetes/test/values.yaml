# sample-color-app-chart/values.yaml
color: "red"
resources:
  requests:
    cpu: 0.1
    memory: 10M
  limits:
    memory: 20M
kind: canary
strategy:
  blueGreen:
    config:
      autoPromotion: true
      autoPromotionSeconds: 30
      scaleDownDelaySeconds: 30
autoscaling:
  minReplicas: 10
  maxReplicas: 10
