# sample-color-app-chart/values.yaml
color: "red"
resources:
  requests:
    cpu: 50mi
    memory: 40M
  limits:
    memory: 51M
kind: canary
strategy:
  blueGreen:
    config:
      autoPromotion: true
      autoPromotionSeconds: 30
      scaleDownDelaySeconds: 30
autoscaling:
  minReplicas: 10
  maxReplicas: 10
