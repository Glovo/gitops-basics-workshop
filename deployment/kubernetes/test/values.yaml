# sample-color-app-chart/values.yaml
color: "red"
resources:
  requests:
    cpu: 50m
    memory: 40Mi
  limits:
    memory: 51Mi
kind: canary
strategy:
  blueGreen:
    config:
      autoPromotion: true
      autoPromotionSeconds: 30
      scaleDownDelaySeconds: 30
autoscaling:
  minReplicas: 10
  maxReplicas: 10
